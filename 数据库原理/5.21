
---

## 一、无损连接分解的判定

### 1. 两种判定方法概述

这个就相当于是有一个原始的关系模式。还有基于给定的函数依赖集，如果对它做了相应的分解，就会涉及到一个原始的关系模式和我们给出的分解方案ρ。要判断给定的这个分解方案是否是无损连接，我们就要根据具体的情况选择方法。

- **分解成多个模式（超过两个）**：一般会用到**表格分析法**。比如说这里有五个模式：AD、AB、BD、CDE、AE，五个模式就是多个，那就要用表格分析法。说白了就是简单无脑的一个分析方法，但理解起来主要还是要把这个表格的建立到最后更新过程搞清楚，理解清楚以后，建立和更新其实没有什么难度。
- **分解成两个子模式**：就是比较简单的情况，可以用**连接分析法**（快速定理判定法），这个后面还会再提到。

---

### 2. 表格分析法（多模式分解）

#### （1）建立初始表格

我们还是回到这里，优先先建一个表格。

**表头与行标识：**

大家先看一下，第一行就是我们的属性，ABCDE，对应的其实就是我们总的关系模式里涉及到的所有属性。我们把所有属性列在第一行，第一行一般就是属性行，然后每一列就对应我们的属性。

第一列的每一行，这里就是AD、AB、BD、CDE、AE，这个是什么呢？这就对应我们分解方案里的每一个子模式。比如说第一行是AD，就对应子模式AD；AB对应子模式AB；BD是BCD的子模式；CDE就是这个；AE就是这个。也就是说，一个总的关系模式分解成了五个子模式，表格里面就有五行。每一行的第一个元素，或者说这个名称，对应的是我们的子模式。所以ABCDE和AD、AB这些，就是表格的第一行和第一列，大家首先要清楚。

**填充表格值——a值与b值的判定：**

然后就是表格的主体部分。主体部分所有的值，首先是要分取a值还是取b值。取a还是取b就这么看：看这一行对应的子模式是否包含了某个属性。

- **包含的属性取a值**：比如说第一行是AD，包含了A属性和D属性，那么第一行的第一列（A列）和第四列（D列）就要设成a值，也就是a₁和a₄。
- **不包含的属性取b值**：那如果没有的属性，比如B、C和E，我们就设成b值。

**下标的确定规则：**

- 如果是a值，我们下标就用列号，比如第一列就是1，第二列是2，第三列是3，第四列是4，第五列是5。
- 如果是b值，下标就用行列坐标，比如第一行第二列就是b₁₂，第一行第三列就是b₁₃，依此类推。就比如说这里第一行第二列就是b₁₂，然后第一行第三列是b₁₃，等等就这样。

这就是我们第一步，根据给定的U（属性全集）和ρ（分解方案），建立这样一张表格。这个表格首先要会建立，主要是a和b这个地方要分清楚。

#### （2）根据函数依赖集F更新表格

建立好了以后，U和ρ的使命就完成了。下面我们主要就是根据这个函数依赖集F来更新表格。

**更新核心规则：**

更新表格的时候，我们把函数依赖集中的每一个函数依赖拿过来，看一遍，做相应的更新。

对于每一个函数依赖，先看它**左边**的属性列。因为b值不可能相等，每个b值的行列索引都不一样，所以一般就是找相等的a值。但要注意，后面会有迭代更新的情况，可能会出现b值相等的情况，所以大家不要默认只有a值相等，b值相等的情况也要考虑。

具体步骤是：先把左边属性列中值相等的所有行找出来（不管是a值相等还是b值相等），然后看这些行在**右边**属性列上的值。如果有不一样的：
- **优先看是否有a值**：如果其中有a值，就把所有值统一更新为这个a值。
- **没有a值全是b值**：如果没有a值，只有b值，就把所有b值更新成其中**下标最小**的那个b值。

**具体例子演示：**

比如说第一个函数依赖，**A→C**。

我们先看左边属性A所在的列，找出A列里所有相等的值。我们看到第1行、第2行和第5行的A列都是a₁，它们相等。找到这些行后，我们就看右边属性C所在的列。这几行的C列值分别是b₁₃、b₂₃和b₅₃，都是b值，而且不一样。对于右边出现不一样的情况，因为没有a值，所以就把这些b值更新成其中行号最小的那个。这里最小的是b₁₃，所以把b₂₃和b₅₃都更新成b₁₃。这就是第一个函数依赖A→C的更新过程。

接下来看第二个函数依赖，**B→C**。

先看左边属性B所在的列，有没有相等的值。我们看第2行和第3行的B列都是a₂，它们相等。然后看这两行右边的C列，当前分别是b₁₃（之前更新过的）和b₃₃。没有a值，所以把b₃₃更新成下标最小的b₁₃。这样更新就又推进了一步。

再看第三个，**C→D**。

先看C列里所有相等的值。现在C列第1、2、3、5行都是b₁₃，它们相等。然后看这些行对应的D列的值。我们看原表，第1、2、3、5行的D列分别是b₅₄、b₃₄、b₂₄和a₄。这里面有个a₄！按照我们的规则，有a值就优先全改成a值，所以把所有b值全部更新成a₄。这样，D列这几行就都变成a₄了。所以在这里就是这样的，这个就是C→D的更新情况。

接着看第四个，**DE→C**。

这里要注意了，左边是DE两个属性的组合，要检查DE这两列的值是否**同时相等**。我们看到第3、4、5行，它们的D列都是a₄，E列都是a₅，所以这三行的DE组合值相等。然后看它们右边的C列。第3行C列是b₁₃，第4行是a₃，第5行是b₃₃。因为有a₃，所以优先更新，把b₁₃和b₃₃都改成a₃。

然后是第五个，**CE→D**。

这个也是类似DE→C的情况，左边是C和E两个属性的组合。看C和E列的组合，第3、4、5行的C和E组合相等（都是a₃和a₅）。再看右边的D列，这三行的D列都是a₄，已经相等了，就不需要更新了。

**关于迭代更新的说明：**

到这里为止，函数依赖集里的所有依赖都看完一遍了。但这里有一个重要的问题需要强调：关于迭代更新。有同学问，所有依赖看完一遍以后，是否还可以再看一遍，看过的能不能再执行。**答案是可以反复执行的。**

比如说，第一遍所有依赖都更新完了，如果还没有出现全a行，你可以再从头把每个依赖再过一遍，看看基于新的表格值，会不会引发新的更新。就是相当于如果是没有全a行的话，你可以再尝试一下是否之前的还可以再通过更新来改变。比如说AB→C、C→D、DE→C这些都更新完了，第一遍流程以后发现没有全a行，你可以再尝试看一下原来更新过的这些函数依赖，是否还能造成额外的更新。

可以根据情况进行反复更新。这个反复执行就是没有限制说每个函数依赖只能迭代一次，理论上是可以迭代多次的。你可以再返回看一下是否还有额外的更新。但是当考试的时候一般不会给你一个这么复杂的情况，让你迭代一遍基本上就出来了。如果没有全a行，那可能就是没有了，不用担心迭代多次的复杂情况出现。

#### （3）判断是否为无损连接

当所有函数依赖应用完，且表格不再更新后，检查表格中是否存在**一整行全部为a值**（如a₁, a₂, a₃, a₄, a₅）。

按照上面的更新过程，我们发现第3行现在是a₁, a₂, a₃, a₄, a₅，**全a行出现了**。这样就可以判定这个分解是**无损连接**的。

所以整个表格分析法的过程就是：先建立表格（a值和b值分清楚），然后拿每一个函数依赖过来更新（左边找相等行，右边做统一），最后看有没有全a行。如果有，就是无损连接。这个从建立和更新到判断的过程，大家一定要清楚，知道以后整个分析就是比较简单直接的一个过程，说白了就是这个意思。主要是需要搞清楚它的规则，搞清楚以后，所有过程就非常顺畅。

---

### 3. 快速定理判定法（两模式分解）

接下来还是回到无损连接的定义。当时还有一个除了表格分析法之外的方法，就是当把一个关系模式**只分解成两个关系模式**的时候。

在这种情况下，我们基本上不需要基于表格分析法，因为可以直接基于一个定理进行判定，其实就比较清楚了。这个定理是说：如果把一个关系模式R分解成R₁和R₂两个子模式，那么满足以下**四个条件之一**，就能判定这个分解是**无损连接**的：

- `R₁ ∩ R₂ → R₁`
- `R₁ ∩ R₂ → R₂`
- `R₁ ∩ R₂ → (R₁ - R₂)`
- `R₁ ∩ R₂ → (R₂ - R₁)`

简单来说，就是两个子模式的**属性交集**，能够函数决定其中一个子模式，或者决定它们之间的某个差集，就可以判定是无损连接。

这个相当于是无损连接的一个充分条件。这个方法比较快，但只适用于分解成两个子模式的情况。如果是涉及到多个子模式的分解，我们还是老老实实用表格分析法。

---

## 二、保持依赖的判定

接下来我们说一下保持依赖。判定一个分解是否是一个好的分解方案，一个是看它是否是无损连接，另外一个就是看是否**保持依赖**。保持依赖相对来说比无损连接要简单一些。

### 1. 函数依赖的投影

首先要搞清楚什么是函数依赖的投影。函数依赖的投影是针对子模式来定义的：函数依赖集F在一个子模式Rᵢ上的投影，指的是在F的闭包F⁺中，所有属性**只包含在Rᵢ中**的函数依赖的集合。

比如说，给定一个关系模式R(ABC)，F = {A→B, A→C}。那么：
- F在R₁(AB)上的投影，就是F⁺中所有只包含AB属性的函数依赖集合，其中包括A→B。
- F在R₂(AC)上的投影，就是F⁺中所有只包含AC属性的函数依赖集合，其中包括A→C。

### 2. 保持依赖的定义

那么保持依赖分解怎么定义呢？它的充分必要条件就是：**所有子模式上的投影的并集，它的闭包等于原函数依赖集F的闭包。**

意思就是说，把所有子模式上各自的函数依赖合在一起，如果它们合起来能推导出原来所有的函数依赖（没有丢失任何依赖关系），那就是保持依赖的分解。如果合起来推不出来原来的某些依赖，那就不是保持依赖，说明有函数依赖在分解过程中丢失了。

### 3. 举例说明

我们结合一个例子来看。有一个关系模式R(ABC)，F = {A→B, B→C}。另外F⁺其实还隐含着A→C（这个还比较直观）。

现在有两种分解方案：

**方案一：R₁(AB), R₂(BC)**
- F₁（在AB上的投影）包含A→B
- F₂（在BC上的投影）包含B→C
- F₁ ∪ F₂ = {A→B, B→C}，它的闭包包含A→C，所以等于F⁺。这个方案**是保持依赖**的。

**方案二：R₁(AB), R₂(AC)**
- F₁（在AB上的投影）包含A→B
- F₂（在AC上的投影）包含A→C
- F₁ ∪ F₂ = {A→B, A→C}，它的闭包能否推出B→C呢？推不出来。B→C丢失了。所以这个方案**不是保持依赖**的。

这里就体现出来了：方案二虽然可能也是无损连接的，但它丢失了B→C这个函数依赖，所以不是保持依赖的。保持依赖的判定就比较简单，就是看分解后所有子模式上的依赖并集是否能覆盖原来所有的依赖关系。

---

## 三、模式分解算法（BCNF与3NF）

接下来要介绍两个综合性的分解算法。如果关系模式不满足范式要求，就需要进行分解。BCNF分解和3NF分解是两种主要的分解算法。

这里需要注意的是，这两种分解算法可以认为是**不兼容**的。因为BCNF分解不一定能够保持函数依赖，它有可能会丢失一定的函数依赖。而3NF分解是既保证无损连接又保证保持依赖的。这就是为什么有时候只能在BCNF和3NF之间做取舍——看你是更关注BCNF的消除冗余性，还是更关注保持函数依赖。

### 1. BCNF分解算法

BCNF分解相对来说比较直观。它其实就是一个**迭代的过程**。

**算法步骤：**

- 首先检查当前的关系模式R是否满足BCNF。
- 如果不满足，那它一定存在某个函数依赖α→β不满足BCNF的条件（即α不是超码）。
- 找出这个违反BCNF的函数依赖α→β。
- 把R分解成两个子模式：
    - `R₁ = (αβ)`：把α和β单独拎出来作为一个子模式。
    - `R₂ = (R - β)`：把β从R中去掉，但保留α。这里特别注意，R₂是去掉β而不是去掉α，因为α是连接点，要保留在R₂中。
- 分解完了以后还没完，这是一个迭代的过程。R₁肯定是满足BCNF的（因为它只有两个属性，或者α就是它的超码）。但要看R₂是否满足BCNF。如果R₂不满足BCNF，就继续对R₂进行分解，找出R₂中违反BCNF的函数依赖，继续分解下去，直到所有子模式都满足BCNF为止。

**关于α∩β的情况：**

- 如果α∩β等于空集，那么R₂可以简化成去掉β后的形式。
- 但R₁和R₂之间是有交集的，这个交集就是α，也就是那个不满足BCNF的函数依赖的左边部分。

**举例说明：**

假设有关系模式R(ABC)，F = {A→B, B→C}。之前已经证明过，这个R不满足BCNF。

- 找到违反BCNF的依赖，比如B→C（B不是超码）。
- 分解：R₁ = (BC)，R₂ = (R - C) = (AB)。
- 然后检查AB上的函数依赖（F在AB上的投影）。AB里面有A→B，A是超码，所以AB满足BCNF。分解结束。

这就是BCNF分解的过程。就还是有一些更复杂的情况，但基本思想就是这个迭代过程。BCNF分解我们不做过多强调，只要能够搞清楚它的具体过程就行。

### 2. 3NF分解算法（基于正则覆盖）

接下来就是3NF分解，这个是跟**正则覆盖**挂钩的。正则覆盖是3NF分解的基础。

#### （1）正则覆盖的定义

正则覆盖是什么？说白了，就是对给定的函数依赖集F做一个"瘦身"，去掉一些冗余的属性。定义是这样的：如果一个函数依赖集Fc满足以下条件，就叫做F的正则覆盖：
- Fc的闭包等于F的闭包。
- Fc中不包含任何**无关属性**。

**无关属性的概念：**

如果从一个函数依赖α→β中去除某个属性A之后，不会改变这个函数依赖集的闭包，那么就称这个属性A是**无关的**。无关属性进一步分为**左无关**和**右无关**：

- **左无关**：在左边α中的一个属性是无关的。判定方法：去掉这个属性A后，看(α-A)→β是否属于原F的闭包。如果属于，说明A是左无关的。
- **右无关**：在右边β中的一个属性是无关的。判定方法：把这个属性A从右边去掉，得到α→(β-A)，然后看这个新的依赖是否和原依赖集等价（即原依赖α→β是否属于更新后的依赖集的闭包）。

**左无关和右无关判定法则的区别：**

这里有一个微妙的区别需要注意：
- **左无关**的判定：是看**更新后的函数依赖**是否属于**原函数依赖集**的闭包。
- **右无关**的判定：是看**原函数依赖**是否属于**更新后的函数依赖集**的闭包。

这个逻辑上有一个方向的区别，大家要稍微注意一下。但总体思想是一样的，就是从函数依赖集中尝试去掉某些属性，看闭包是否改变，不改变就说明这个属性是无关的，可以去掉。

**关于正则覆盖的补充说明：**

正则覆盖的求解，就是不断地检查每一条函数依赖的左边和右边有没有无关属性，有就去掉。需要注意的是，正则覆盖**不一定是唯一的**——你按照不同的顺序处理函数依赖，可能得到不同的正则覆盖。但不同的正则覆盖之间是等价的，对后续的3NF分解结果可能会有影响，但都是正确的。

#### （2）3NF分解的步骤

有了正则覆盖Fc之后，3NF分解的步骤如下：

**第一步：为每个依赖建立子模式**

对正则覆盖Fc中的每一条函数依赖α→β，都单独建立一个子模式(αβ)。就是说把每一条依赖单拎出来，作为一个关系子模式。这就相当于一个for循环。

**第二步：检查并补充候选码**

这一步很关键。做完第一步之后还没完，还要判断一下：原始关系模式R的**候选码**，是否已经包含在某个已经创建的子模式中了？

- 如果某个子模式已经包含了候选码，那就OK，不需要额外添加。
- 如果**没有任何一个子模式包含候选码**，那么就需要额外再单独建立一个子模式，专门用来放这个候选码。

这一步的目的是保证分解是无损连接的。3NF分解分解出来的结果，一定是既满足3NF，又满足无损连接，又保持依赖的。

**举例说明：**

假设有关系模式R(ABCDE)，正则覆盖Fc = {A→BC, B→A, C→D}。

- 第一步：建立三个子模式——R₁(ABC)，R₂(BA)即(AB)，R₃(CD)。可以合并R₁和R₂为(ABC)。
- 第二步：假设R的候选码是E（或者包含E的组合），而上面三个子模式里都没有包含E。那就需要额外再建立一个R₄，把候选码放进去。

这个过程大家一定要清楚，就是我们每一个函数依赖单拎出来作为一个关系子模式，最后再判定一下当前分解里面是否包含了原关系模式中的候选码。如果不包含，就再单独额外建立一个子模式。

---

### 3. BCNF与3NF的总结对比

- **BCNF分解**：保证无损连接，但不一定能保持依赖。用迭代法，把每个违反的依赖单独拎出来分解。
- **3NF分解**：保证无损连接，也保证保持依赖。基于正则覆盖，为每个依赖建一个子模式，最后补候选码。
- **取舍**：在实际应用中，需要在BCNF和3NF之间做权衡。如果你更看重消除所有冗余（BCNF），可能会丢失一些依赖；如果你更看重保持所有业务规则（函数依赖），就选择3NF。

---

## 四、考试与应用要点

### 1. 典型考题形式

期末考试中，这个章节的题目形式一般是这样的：

- **第一小题**：给定一个关系模式和分解方案，判断是否是无损连接、是否保持依赖。这就是用我们前面讲的表格分析法或者快速定理来判定。
- **第二小题**：给一个业务场景，要求：
    1.  先分析出函数依赖集，建立原始的关系模式。
    2.  判定这个关系模式最高满足第几范式（1NF、2NF、3NF还是BCNF）。一般情况下，可能是最高满足2NF或者3NF，因为有部分依赖或传递依赖的问题。
    3.  然后要求给出一个分解方案，达到BCNF或者3NF。如果是3NF，一般会涉及正则覆盖和3NF分解流程。

### 2. 关于正则覆盖的考试要求

在考试中，求正则覆盖这部分，一般不会让大家做太复杂的运算。通常情况下，给出的函数依赖集本身就是比较精简的，一眼能看出是最精简的、瘦身过的版本。所以大家可以基于给定的函数依赖集直接进行3NF分解。

但是对于流程一定要清楚：从正则覆盖怎么求到对应的3NF分解——每一条依赖单拎出来作为一个子模式，最后判定是否包含候选码，不包含就额外增加一个。这个过程大家必须掌握。

### 3. 场景分析题的难点

这类题的真正难点，其实不在于表格分析法或者分解算法本身，而在于**基于场景分析出函数依赖集**。

比如说，给一个课程与教师的场景，我们要能分析出来这样的函数依赖：
- 课程号→课程名、学分、学时
- 教工号→教师姓名、职称、学位
- (课程号, 教工号)→工作量（或者其他组合属性）

基于这个场景分析出函数依赖集以后，再看存在什么问题：可能存在部分依赖（那就最高是2NF），可能存在传递依赖（那就最高是2NF或3NF），然后根据要求做BCNF分解或3NF分解。

对于BCNF分解，思路比较直观，就是把不满足的依赖单拎出来，剩下的继续分解。对于3NF分解，基于分析出的函数依赖集直接做就行（一般考试时不需要再额外求正则覆盖，直接用给定的依赖即可）。

---

## 五、整体总结

整个关系模式分解这一章节，核心逻辑是这样的：

1.  **判断好坏**：给定一个关系模式，先判断它是否是一个好的模式——即判定它满足第几范式（1NF、2NF、3NF、BCNF）。如果不满足3NF或BCNF，就需要分解。
2.  **判定分解质量**：给定一个分解方案，要判定它是否是好的分解。两个标准——**无损连接**（表格分析法或快速定理）和**保持依赖**（投影并集判定法）。
3.  **执行分解**：如果不满足范式要求，通过分解算法得到好的分解。
    -   **BCNF分解**：迭代法，不满足就分解，保证无损，不一定保持依赖。
    -   **3NF分解**：正则覆盖→建子模式→补候选码，保证无损且保持依赖。

整个过程大家课下一定要结合例子多练习。表格分析法从建立到更新，BCNF的迭代分解流程，3NF基于正则覆盖建子模式再补候选码的流程——这些必须搞清楚。如果有不清楚的，一定要来问，一定要把该掌握的都掌握清楚。
